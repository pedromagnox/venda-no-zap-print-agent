# URL base do backend Venda no Zap.
# - Dev: aponta pro mock local (ver src/lib/api/mock-backend.ts), default http://localhost:4317
# - Prod: https://vendanozap.app
PRINT_AGENT_API_BASE_URL=http://localhost:4317

# Ativa o mock backend embutido (apenas em dev). false em produção.
PRINT_AGENT_USE_MOCK=true
